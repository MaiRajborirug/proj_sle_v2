# Copy to .streamlit/secrets.toml and fill in. That filename is gitignored;
# this .example file is committed and must never contain a real key.
#
# The same two values go into Streamlit Community Cloud (Advanced settings → Secrets,
# paste this TOML) and into Render (Environment → two separate variables).

SHEET_ID = "1aZk3M3mbnNIsFrHxvocgP25lwkcP1s8w-G8K6sFy5mY"

# Paste the entire downloaded service-account JSON between the triple quotes,
# including the outer braces. Keep it on multiple lines — that is fine in TOML.
GOOGLE_SERVICE_ACCOUNT_JSON = '''
{
  "type": "service_account",
  "project_id": "...",
  "private_key_id": "...",
  "private_key": "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n",
  "client_email": "sle-recorder@your-project.iam.gserviceaccount.com",
  "client_id": "...",
  "token_uri": "https://oauth2.googleapis.com/token"
}
'''

# Optional. A random salt is generated per restart if this is absent.
# IP_HASH_SALT = "any-long-random-string"
